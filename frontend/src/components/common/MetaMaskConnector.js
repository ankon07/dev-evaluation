import React from 'react';

// Simplified MetaMaskConnector that doesn't require MetaMask
const MetaMaskConnector = () => {
  // This component is now a no-op - it doesn't do anything
  return null;
};

export default MetaMaskConnector;
